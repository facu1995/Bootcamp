const log=console.log;

function arrayInvertido(){
    var aux=[];
    for(var i=array.length-1; i>=0; i--){
        aux.push(array[i]);
    }
    array=aux;
}

let array=[1,2,3,4]
log(array);
log(array);